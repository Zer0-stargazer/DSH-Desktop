# DSH Desktop

Windows 上打开官方 `dsh web` 的窗口。个人开发者 Zer0 做的，不是 DeepSeek 官方客户端。

新版本：https://github.com/Zer0-stargazer/DSH-Desktop/releases

双击 `DSH Desktop.exe`。第一次填自己的 API Key，存在本机（DPAPI）。

数据默认在 `%APPDATA%\DSH Desktop\dsh-home`，和 `~/.dsh` 分开。要共用去设置 → 常规，改完重启。两边不要同时开。

内核在设置 → 内核里跟着 npm 更新。点「同步官方更新」会弹出进度窗口。自带版本可能不是最新。会走系统代理或本机 Clash（127.0.0.1:7897）。这次更新会很慢：依赖大，常要数分钟到十余分钟；进度到 90% 附近往往还在下，条子偶尔会停一下，一般不是崩。刚更新后的第一次打开也会和首次安装一样慢。

点右上角关闭会进托盘，进程还在。要退出：托盘右键「退出」。改完代码后也必须从托盘退出再开，否则还是旧窗口。

窗口或同步有问题：到 https://github.com/Zer0-stargazer/DSH-Desktop/issues 开一条，或设置 → 诊断 → 导出报告后发 gl20070126@gmail.com。不要发 Key。
Agent / 插件 / 模型：官方 Discussions。

初心和声明在「关于」里。

未签名。Windows 可能提示未知发布者。
